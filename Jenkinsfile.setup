pipeline {
    agent { label 'electronix'}

    stages {
        stage("Tools Configure"){
            steps { echo "Tools Installation Pipepline" }
        }
        stage ("List Update"){
            steps {
                sh '''
                sudo apt-get update -y
                '''
            }
        }
    }

}